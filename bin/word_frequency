#!/bin/bash
# 
# Take the minified version
# Remove characters not valid in identifiers
# Remove single-character identifiers, common in minified code
# Sort, so the identifiers are next to each other
# Get the counts of each identifier
# Sort numerically
cd "$(dirname "$0")"/..
cat jquery.uniform.min.js | \
	tr -sc "a-zA-Z0-9" "\n" | \
	sed "/^.$/d" | \
	sort | \
	uniq -c | \
	sort -n
