#!/bin/bash

cd "$(dirname "$0")"/..

if [ -z "$1" ]; then
	echo "Specify a single filename"
	exit
fi

curl -s --header "Content-Type: text/plain" --data-binary "@$1" https://api.github.com/markdown/raw
